{
  "name": "kmch-robotic-surgery-camp",
  "version": "1.0.0",
  "private": true,
  "description": "KMCH robotic surgery consultation camp landing page",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.mjs",
    "dev": "node --env-file-if-exists=.env server.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
